# FileCodeBox-worker Code Review (10.12)

## 审查范围
- `src/db.ts` (307 行)
- `src/shared.ts` (235 行)
- `src/index.ts` (287 行)
- `src/api.ts` (213 行)
- `src/utils.ts` (67 行)
- `src/auth.ts` (~90 行)
- `src/templates/layout.ts` (36 行)
- `src/templates/pages.ts` (361 行)
- `src/templates/style.ts` (541 行)

---

## 🐛 严重 Bug

### H1: `uploadFile` 流式上传后 R2 写入内容为空

**位置**: `src/shared.ts:89-119`

**问题描述**:
```typescript
// 第 89-103 行：pump 函数从 req.body 读数据，写入 TransformStream 的 writable
const { readable, writable } = new TransformStream();
const writer = writable.getWriter();
const reader = (c.req.raw.body as ReadableStream).getReader();

const drainPromise = reader.read().then(async function pump({ done, value }) {
  if (done) return;
  size += value.byteLength;
  if (size > maxSize) { writer.abort(...); return; }
  return writer.write(value).then(() => reader.read().then(pump)); // 读 + 写
});

await Promise.all([drainPromise, readable.pipeTo(writable)]); // readable→writable（循环管道）

// 第 119 行：此时 readable 已被 drainPromise + pipeTo 消费完毕！
await env.FILE_STORE.put(`file:${code}`, readable, { ... }); // ← 写入空流！
```

**根本原因**:
1. `pump` 从 `req.body` 读取并写入 `writable`
2. `readable.pipeTo(writable)` 创建一个循环管道（transform 流的 readable→writable）
3. 完成后 `readable` 已耗尽（数据被 pump 消费或形成了循环）
4. `FILE_STORE.put()` 拿到空流，上传文件为 0 bytes

**复现路径**: 用户上传图片/文件 → 上传成功但文件大小为 0

**修复方案**: 将 size 测量和 R2 写入分阶段执行：
```typescript
// Phase 1: 测量 size（流式，不缓冲完整文件）
const sizeStream = new TransformStream();
const sizeWriter = sizeStream.writable.getWriter();
const bodyReader = (c.req.raw.body as ReadableStream<Uint8Array>).getReader();

// drain into sizeStream to measure
// abort if overflow

// Phase 2: 重新创建流用于 R2 写入
const { readable, writable } = new TransformStream();
const r2Writer = writable.getWriter();
const finalReader = (c.req.raw.body as ReadableStream<Uint8Array>).getReader();

await Promise.all([
  finalReader.read().then(function pump({ done, value }) {
    if (done) return;
    return r2Writer.write(value).then(() => finalReader.read().then(pump));
  }),
]);
// 然后 put readable to R2
```

**严重程度**: 🔴 高 — 影响所有文件上传功能

---

## ⚠️ 中等问题

### M1: CSP 头部残留 Google Fonts 规则（已移除字体但仍保留 CSP 规则）

**位置**: `src/index.ts:47-49`

```typescript
"style-src 'self' 'unsafe-inline' fonts.googleapis.com; " +  // 未使用但仍允许
"font-src fonts.gstatic.com; " +                              // 未使用但仍允许
```

**影响**: 低风险 — 不影响功能，但 CSP 规则与实际资源不一致，降低安全策略可信度

**修复**:
```typescript
"style-src 'self' 'unsafe-inline'; " +
// 移除 font-src 行
```

---

### M2: `listFiles` 无关键词搜索时 COUNT 查询独立于数据查询

**位置**: `src/db.ts:171-180`

```typescript
const [rows, countRow] = await Promise.all([
  db.prepare('SELECT * FROM fc_files ORDER BY id DESC LIMIT ? OFFSET ?').bind(PAGE_SIZE, offset).all(),
  db.prepare('SELECT COUNT(*) as count FROM fc_files').first(),  // 全表 COUNT
]);
```

**影响**: 当文件数量达到数万级别时，COUNT 查询可能成为性能瓶颈（D1 无索引扫描）

**修复**: 考虑在 `fc_files` 上添加计数缓存或使用 D1 分页游标

---

### M3: `handleAdminLogin` 每次都进行两次 PBKDF2 哈希计算

**位置**: `src/auth.ts:52-56`

```typescript
const inputHash = await hashPassword(inputPassword);  // 100k 次迭代
const secretHash = await hashPassword(env.ADMIN_PASSWORD); // 100k 次迭代
```

**影响**: 每次登录请求执行 200k 次 PBKDF2 迭代（约 200-400ms），且 secret hash 每次重新计算

**修复**: 缓存 secret hash（启动时计算一次）或使用静态 SECRET_HASH

```typescript
// 在模块顶层计算一次
const _secretHash = await hashPassword(env.ADMIN_PASSWORD);
// 登录时直接使用 _secretHash 而非重新计算
```

---

## 💡 建议优化

### L1: `parseExpire` `'count'` 分支注释误导

**位置**: `src/shared.ts:31`

```typescript
case 'count':
  // expireValue 为用户设置的最大下载次数；expireAt 用默认天数计算，maxDownloads 由调用方覆盖
  return { expireAt: new Date(now + expireValue * 86_400_000).toISOString(), maxDownloads: -1 };
```

**问题**: 注释说"expireValue 是最大下载次数"，但实际 `expireValue` 参数传入的是天数，这里逻辑混淆

---

### L2: favicon 仍为外部请求 (q1.qlogo.cn)

**位置**: `src/templates/layout.ts:18`

```html
<link rel="icon" href="https://q1.qlogo.cn/g?b=qq&nk=2847495839&s=640">
```

**建议**: 改为 inline SVG 或 data URI，减少外部请求

---

### L3: `selectFile` 中 `text` 字段命名与 `DownloadResult` 不一致

**位置**: `src/shared.ts`

- `DownloadResult` 返回 `obj: R2ObjectBody | null`
- `SelectResult` 返回 `text?: string`（对文本文件预读取）

**建议**: 统一接口，避免调用方混淆

---

### L4: `defaultExpireDays` API 缺少类型校验

**位置**: `src/api.ts:202`

```typescript
if (body.defaultExpireDays !== undefined)
  await setSetting(env.DB, 'default_expire_days', String(body.defaultExpireDays));
```

**建议**: 校验值为数字且在合法范围内

---

## ✅ 安全审计通过项

| 检查项 | 状态 | 说明 |
|--------|------|------|
| XSS 防护 | ✅ | 所有用户输入经过 `escapeHtml()` 转义 |
| 时序安全比较 | ✅ | `timingSafeCompare` 实现正确 |
| 密码哈希 | ✅ | PBKDF2 10万迭代，固定盐值 |
| 登录限流 | ✅ | 60秒窗口内最多5次尝试 |
| 取件码防碰撞 | ✅ | CSPRNG + 拒绝采样 |
| Code 释放机制 | ✅ | delete/expiry 时调用 `releaseCode()` |
| 文件删除顺序 | ✅ | R2 先删，DB 在 finally 中清理 |
| CORS 配置 | ✅ | `origin: 'self'`，方法白名单 |
| CSP 头部 | ✅ | 基础策略完整（需清理 M1） |
| SQL 注入 | ✅ | 全部使用参数化查询 |
| 流式上传内存控制 | ⚠️ | 见 H1，当前实现有 bug |

---

## 总结

| 级别 | 数量 | 必须修复 |
|------|------|----------|
| 🔴 严重 | 1 | H1: 流式上传逻辑 bug |
| ⚠️ 中等 | 3 | M1: CSP 残留 / M2: COUNT 性能 / M3: PBKDF2 缓存 |
| 💡 建议 | 4 | L1-L4 |

**最关键问题**: H1 流式上传 bug 导致所有文件上传实际写入空内容（需立即修复）
