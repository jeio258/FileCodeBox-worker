# Code Review — 2026-08-28（第 3 轮）

> 项目：FileCodeBox-worker（Cloudflare Workers + D1 + R2）
> 范围：本轮综合审查 + 已修复问题验证

---

## 本轮新增修复

| # | 问题 | 修复方式 |
|---|------|----------|
| H2-fix | `api.ts` `/file/delete` 端点孤儿文件风险 | 改为先删 R2、`deleteFileRecord` 放 `finally`，与 index.ts 管理后台一致 |
| H1-fix | `retrievePage` 中取件码未 HTML 转义 | `value="${code ?? ''}"` → `value="${escapeHtml(code ?? '')}"` |

---

## 本轮发现新问题的能力

### 🔴 高优先级（已修复）

**H1 — `retrievePage` 中取件码未转义，存在反射型 XSS**
```typescript
// src/templates/pages.ts:159（已修复）
// ❌ 修复前
value="${code ?? ''}"
// ✅ 修复后
value="${escapeHtml(code ?? '')}"
```
`code` 来自 URL 查询参数，恶意构造的 URL 可使 `<input>` 的 value 属性被闭合，注入任意 HTML/JS。

**H2 — `api.ts` 的 `/file/delete` 端点有孤儿文件风险**
```typescript
// src/api.ts:175-177（已修复）
// ❌ 修复前：先删 DB + 释放取件码，再删 R2，R2 失败则产生孤儿
await deleteFileRecord(env.DB, id);
await env.FILE_STORE.delete(`file:${file.code}`);
// ✅ 修复后：先删 R2，DB 删除放 finally 保证执行
try {
  await env.FILE_STORE.delete(`file:${file.code}`);
} finally {
  await deleteFileRecord(env.DB, id);
}
```

---

### 🟡 中优先级（待处理或可接受）

**M1 — `parseExpire` 的 `'count'` 分支注释有误导性**
```typescript
// src/shared.ts:31-32
case 'count':
  // expireValue 在此作为 defaultDays；maxDownloads 由调用方覆盖
  return { expireAt: new Date(now + expireValue * 86_400_000).toISOString(), maxDownloads: -1 };
```
`expireValue` 在 `'count'` 分支中实际上是**天数**（用于计算 expireAt），而非 `defaultDays`。`maxDownloads` 由调用方通过 `effectiveMaxDownloads` 覆盖。注释应改为：
```typescript
case 'count':
  // expireValue 为用户设置的次数上限；expireAt 用默认天数计算，maxDownloads 由调用方覆盖
```

**M2 — `ip` 字段从未被写入**
`fc_files.ip` 列在表结构中定义，但 `insertFile()` 的 INSERT 语句未包含该列，导致所有记录的 IP 始终为空字符串。建议：在 `insertFile` 调用处传入客户端 IP（来自 `CF-Connecting-IP` / `x-forwarded-for`），或从表结构中移除该列。

**M3 — `uploadFile` 改用 `arrayBuffer()` 增加内存峰值**
```typescript
// src/shared.ts:85
const body = await c.req.arrayBuffer();  // 将整个文件加载到内存
```
原来使用 `c.req.raw.body`（ReadableStream）直接流式写入 R2，不占用额外内存。现在改为 `arrayBuffer()` 后，对于接近大小限制的 100MB 文件，Worker 内存峰值将增加 100MB。
- Cloudflare Workers 内存限制：Free 128MB，Pro $0.50/100k 请求起
- 在 100MB 文件场景下可能接近上限
- **建议**：短期可接受（安全优先），长期可考虑实现流式长度测量（边读边计数，达到上限时 abort）

---

### 🟢 无需修复（已验证）

| 项目 | 状态 |
|------|------|
| `errorPage(title, message)` 参数 | ✅ 所有调用方传入硬编码中文字符串，无用户可控数据 |
| `pages.ts` 中 `${file.code}` / `${f.id}` URL 拼接 | ✅ 取件码纯数字，id 为整数，无注入风险 |
| `pages.ts` 中 `error` 变量 | ✅ 来自 `adminLoginPage(error?)` 参数，调用方均为硬编码 |
| `cleanupExpired` 孤儿风险 | ✅ `Promise.all(R2删除)` 整体 reject 时后续 DB 删除不执行，无孤儿 |
| `ensureCodeAvailable` 双重 `isCodeTaken` | ⚠️ 自动生成码已有 `generateUniqueCode` 保证唯一，再查一次冗余但不影响正确性 |
| CSP `script-src 'self' 'unsafe-inline'` | ✅ 因页面大量内联 JS 所必需，暂时可接受 |

---

## 已修复问题总览（累计）

| 优先级 | 问题 | 状态 |
|--------|------|------|
| 🔴 上轮 | `uploadFile` max_downloads 被忽略 | ✅ 已修复 |
| 🔴 上轮 | `Content-Length` 伪造绕过大小限制 | ✅ 已修复 |
| 🔴 上轮 | 管理后台删除时 R2 孤儿文件 | ✅ 已修复 |
| 🔴 本轮 | `api.ts` `/file/delete` 孤儿文件 | ✅ 已修复 |
| 🔴 本轮 | `retrievePage` 取件码 XSS | ✅ 已修复 |
| 🟡 上轮 | `uploadFile`/`uploadText` 重复代码 | ✅ 已提取 `ensureCodeAvailable` |
| 🟡 上轮 | `parseExpire` 冗余参数 | ✅ 已移除 `defaultDays` |
| 🟡 本轮 | `parseExpire` 注释误导 | ⏳ 建议修正 |
| 🟡 本轮 | `ip` 字段未写入 | ⏳ 建议补充 |
| 🟡 本轮 | `arrayBuffer()` 内存峰值 | ⏳ 可接受，长期可优化 |

---

## TypeScript 编译：✅ 零错误
